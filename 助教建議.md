同學午安～

這邊後來有再細看跟了解原因哩，
依照同學目前的撰寫跟判斷，在 .env 的格式需為：
API_PATH="livejs/v1/customer/xxxxxxx"
API_KEY=xxxxxxxxxxxxxx


而一般撰寫跟助教的測試，通常會使用：
API_PATH=xxxxxxx
API_KEY=xxxxxxxxxxxxxx


然後搭配如下的 URL 寫法：
${BASE_URL}/api/livejs/v1/customer/${API_PATH}/products
${BASE_URL}/api/livejs/v1/admin/${API_PATH}/orders

所以先前測試同學的作業時，才會產生未通過的狀況。
同學的 .env 搭配 URL 寫法也是可行的，實際會通過測試，這個部分助教看得不夠仔細，不好意思 > <

不過也比較建議同學使用一般的撰寫方式，這樣可以省略如下的路徑判斷唷～

const m = String(API_PATH).match(/customer\/(.+)$/);
  const url = m
    ? `${BASE_URL}/api/livejs/v1/admin/${m[1]}/orders`
    : `${BASE_URL}/api/${API_PATH}/admin/orders`;
// ...


------

整體程式碼測試沒有問題；
不過任務四的比較題（說明 fetch 和 axios 的主要差異）有缺漏，所以目前無法調整為審核成功。
因為第七週審核上除了程式碼測試之外，也包含了比較題的撰寫（使用註解撰寫）～

如果同學能在這週三 5/20 晚上 22:00 之前補上相關撰寫，
並更新到 Repo：https://github.com/113120067/js-camp-week7
那麼助教會再協助調整審核（成功）。

或者這一兩天同學補上後，就可以私訊跟我說～～

（比較題提示：從 JSON 解析跟錯誤處理切入）